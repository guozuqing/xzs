# 考试系统 - 修改后编译部署指南

## 项目结构

```
考试系统/xzs/source/
├── xzs/                          # Java 后端 (Spring Boot)
│   ├── src/main/java/            # Java 源码
│   ├── src/main/resources/       # 配置文件 + MyBatis Mapper XML
│   │   ├── static/student/       # 学生端前端构建产物
│   │   └── static/admin/         # 管理端前端构建产物（如有）
│   └── pom.xml
└── vue/
    ├── xzs-admin/                # 管理端前端 (Vue.js)
    └── xzs-student/              # 学生端前端 (Vue.js)
```

## 环境要求

- **JDK**: 1.8+
- **Maven**: 3.x
- **Node.js**: 12+ (推荐 14.x)
- **npm**: 6+
- **PostgreSQL**: 9.6+

## 数据库配置

数据库连接信息在 `xzs/source/xzs/src/main/resources/application-dev.yml` 中配置：

```yaml
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/xzs
    username: postgres
    password: 123456
```

### 数据库变更（必须执行）

如果是首次部署修改后的版本，需要执行以下 SQL：

```bash
PGPASSWORD=123456 psql -h localhost -U postgres -d xzs -c "
CREATE TABLE IF NOT EXISTS t_exam_config (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255),
    subject_id INTEGER,
    question_count INTEGER,
    score INTEGER,
    suggest_time INTEGER,
    pass_score INTEGER DEFAULT 0,
    create_user INTEGER,
    create_time TIMESTAMP,
    deleted BOOLEAN DEFAULT false
);"
```

如果 `t_exam_config` 表已存在但缺少 `pass_score` 字段：

```bash
PGPASSWORD=123456 psql -h localhost -U postgres -d xzs -c "ALTER TABLE t_exam_config ADD COLUMN IF NOT EXISTS pass_score INTEGER DEFAULT 0;"
```

---

## 编译部署步骤

### 第一步：编译学生端前端

```bash
cd /home/skydomain/考试系统/xzs/source/vue/xzs-student
npm install          # 首次或依赖变更时执行
npm run build        # 构建产物输出到 ./student/ 目录
```

### 第二步：复制学生端构建产物到后端静态资源

```bash
rm -rf /home/skydomain/考试系统/xzs/source/xzs/src/main/resources/static/student/
cp -r /home/skydomain/考试系统/xzs/source/vue/xzs-student/student/ \
      /home/skydomain/考试系统/xzs/source/xzs/src/main/resources/static/student/
```

### 第三步：编译管理端前端（如果修改了管理端代码）

```bash
cd /home/skydomain/考试系统/xzs/source/vue/xzs-admin
npm install          # 首次或依赖变更时执行
npm run build        # 构建产物输出到 ./admin/ 目录
```

复制到后端：

```bash
rm -rf /home/skydomain/考试系统/xzs/source/xzs/src/main/resources/static/admin/
cp -r /home/skydomain/考试系统/xzs/source/vue/xzs-admin/admin/ \
      /home/skydomain/考试系统/xzs/source/xzs/src/main/resources/static/admin/
```

### 第四步：编译 Java 后端

```bash
cd /home/skydomain/考试系统/xzs/source/xzs
mvn package -DskipTests
```

编译成功后 jar 包位于：`target/xzs-3.9.0.jar`

### 第五步：停止旧服务并启动新服务

```bash
# 停止旧进程
kill $(pgrep -f "xzs-3.9.0.jar") 2>/dev/null
sleep 2

# 启动新服务
cd /home/skydomain/考试系统/xzs/source/xzs
java -jar target/xzs-3.9.0.jar &
```

---

## 一键编译部署脚本

将以下内容保存为 `deploy.sh`，放在 `/home/skydomain/考试系统/` 目录下：

```bash
#!/bin/bash
set -e

BASE_DIR="/home/skydomain/考试系统/xzs/source"

echo "=== 1. 编译学生端前端 ==="
cd "$BASE_DIR/vue/xzs-student"
npm run build

echo "=== 2. 复制学生端构建产物 ==="
rm -rf "$BASE_DIR/xzs/src/main/resources/static/student/"
cp -r "$BASE_DIR/vue/xzs-student/student/" "$BASE_DIR/xzs/src/main/resources/static/student/"

echo "=== 3. 编译管理端前端 ==="
cd "$BASE_DIR/vue/xzs-admin"
npm run build

echo "=== 4. 复制管理端构建产物 ==="
rm -rf "$BASE_DIR/xzs/src/main/resources/static/admin/"
cp -r "$BASE_DIR/vue/xzs-admin/admin/" "$BASE_DIR/xzs/src/main/resources/static/admin/"

echo "=== 5. 编译 Java 后端 ==="
cd "$BASE_DIR/xzs"
mvn package -DskipTests

echo "=== 6. 重启服务 ==="
kill $(pgrep -f "xzs-3.9.0.jar") 2>/dev/null || true
sleep 2
nohup java -jar target/xzs-3.9.0.jar > /tmp/xzs.log 2>&1 &

echo "=== 部署完成！==="
echo "后端日志: /tmp/xzs.log"
echo "管理端: http://localhost:8000/admin/"
echo "学生端: http://localhost:8000/student/"
```

使用方式：

```bash
chmod +x /home/skydomain/考试系统/deploy.sh
/home/skydomain/考试系统/deploy.sh
```

---

## 开发模式（前端热更新）

如果只修改前端代码，可以用 dev server 实时预览，无需每次 build：

**管理端开发服务器（端口 9002）：**

```bash
cd /home/skydomain/考试系统/xzs/source/vue/xzs-admin
npm run serve
```

**学生端开发服务器（端口 8001）：**

```bash
cd /home/skydomain/考试系统/xzs/source/vue/xzs-student
npm run serve
```

> 注意：dev server 会自动代理 `/api` 请求到 `http://localhost:8000`，所以后端必须在运行中。

---

## 访问地址

| 页面     | 地址                              |
| -------- | --------------------------------- |
| 管理端   | http://localhost:8000/admin/      |
| 学生端   | http://localhost:8000/student/    |
| 管理端开发 | http://localhost:9002 (dev模式)  |
| 学生端开发 | http://localhost:8001 (dev模式)  |

---

## 修改后需要重新编译的场景对照表

| 修改内容                     | 需要执行的步骤                         |
| ---------------------------- | -------------------------------------- |
| 只改了 Java 代码             | 第四步 + 第五步                        |
| 只改了学生端 Vue 代码        | 第一步 + 第二步 + 第四步 + 第五步      |
| 只改了管理端 Vue 代码        | 第三步（复制）+ 第四步 + 第五步        |
| 改了 Java + 学生端 Vue       | 第一步 + 第二步 + 第四步 + 第五步      |
| 改了数据库表结构             | 先执行 SQL，再执行对应的编译步骤       |
| 只改了前端（开发模式）       | 无需编译，dev server 自动热更新        |

---

## 常见问题

### Q: 编译 Java 报错？
确保使用 JDK 8，且 `pom.xml` 中的依赖都已下载：
```bash
mvn clean install -DskipTests
```

### Q: 前端 npm install 报错？
尝试清除缓存重试：
```bash
rm -rf node_modules package-lock.json
npm install
```

### Q: 学生端改了代码但页面没变？
学生端前端是打包成静态文件由 Java 后端提供的，修改 Vue 代码后必须：
1. `npm run build`
2. 复制构建产物到 `static/student/`
3. 重新 `mvn package`
4. 重启 Java 服务

### Q: 数据库连接失败？
检查 PostgreSQL 是否运行，以及 `application-dev.yml` 中的数据库连接配置。
