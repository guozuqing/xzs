# 天空领域-培训发展部开源考试系统 - 项目解析文档

> 版本：3.5.0  
> 版权：天空领域培训发展部  
> 许可证：AGPL-3.0

---

## 一、项目概述

天空领域-培训发展部（XZS）是一套基于 **Spring Boot + Vue.js** 的在线考试系统，支持多角色（管理员 / 学生）、多题型、自动阅卷等功能。系统采用前后端分离架构，同时支持**集成部署**（前端静态文件嵌入后端）和**前后端分离部署**两种模式。

### 核心特性

- 多种题型支持：单选题、多选题、判断题、填空题、简答题
- 三种试卷类型：固定试卷、时段试卷、任务试卷
- 自动阅卷与人工批改
- 错题本与考试记录
- 消息通知系统
- 用户事件日志
- 微信小程序接入（学生端）
- Docker 一键部署

---

## 二、技术栈

### 后端

| 技术 | 版本 | 说明 |
|------|------|------|
| Java | 8+ | 运行环境 |
| Spring Boot | 2.1.6.RELEASE | 核心框架 |
| Spring Security | - | 认证与授权 |
| MyBatis | 2.0.1 | ORM 框架 |
| PageHelper | 1.2.12 | MyBatis 分页插件 |
| PostgreSQL | - | 数据库 |
| Undertow | - | 内嵌 Web 服务器 |
| ModelMapper | 2.3.5 | 对象映射 |
| HikariCP | - | 数据库连接池 |

### 前端

| 技术 | 说明 |
|------|------|
| Vue.js 2.x | 前端框架 |
| Vue Router | 路由管理 |
| Vuex | 状态管理 |
| Element UI | UI 组件库 |
| Axios | HTTP 请求 |
| Vue CLI | 脚手架工具 |

---

## 三、项目目录结构

```
xzs/
├── docker/                          # Docker 部署配置
│   ├── docker-compose.yml
│   ├── install/                     # 安装脚本
│   └── release/                     # 发布配置
├── docs/                            # 项目文档截图
├── release/                         # 发布版本（编译后的 jar 等）
├── source/                          # 源码目录
│   ├── xzs/                         # 后端源码（Spring Boot）
│   │   ├── pom.xml
│   │   └── src/main/
│   │       ├── java/com/mindskip/xzs/
│   │       │   ├── XzsApplication.java        # 应用启动入口
│   │       │   ├── base/                      # 基础类（Controller基类、RestResponse、SystemCode）
│   │       │   ├── configuration/             # 配置（Security、属性、异常处理）
│   │       │   ├── context/                   # Web上下文（当前用户管理）
│   │       │   ├── controller/                # 控制器
│   │       │   │   ├── admin/                 # 管理端 API（/api/admin/**）
│   │       │   │   ├── student/               # 学生端 API（/api/student/**）
│   │       │   │   └── wx/                    # 微信端 API（/api/wx/**）
│   │       │   ├── domain/                    # 实体类与枚举
│   │       │   ├── event/                     # Spring 事件
│   │       │   ├── listener/                  # 事件监听器
│   │       │   ├── repository/                # MyBatis Mapper 接口
│   │       │   ├── service/                   # 业务逻辑层
│   │       │   ├── utility/                   # 工具类
│   │       │   └── viewmodel/                 # 视图模型（请求/响应 DTO）
│   │       └── resources/
│   │           ├── application.yml            # 主配置
│   │           ├── application-dev.yml        # 开发环境配置
│   │           ├── mapper/                    # MyBatis XML 映射文件
│   │           └── static/                    # 内嵌前端静态文件
│   │               ├── admin/                 # 管理端前端（编译后）
│   │               └── student/               # 学生端前端（编译后）
│   └── vue/                          # 前端源码
│       ├── xzs-admin/                # 管理端前端
│       │   ├── package.json
│       │   ├── vue.config.js
│       │   └── src/
│       │       ├── api/              # API 接口定义
│       │       ├── components/       # 公共组件
│       │       ├── layout/           # 页面布局
│       │       ├── router.js         # 路由配置
│       │       ├── store/            # Vuex 状态管理
│       │       ├── utils/            # 工具类（请求封装）
│       │       └── views/            # 页面视图
│       └── xzs-student/             # 学生端前端（结构同上）
├── init-db.sql                       # 数据库初始化脚本
├── LICENSE                           # AGPL-3.0 许可证
└── README.md                         # 项目说明
```

---

## 四、系统架构

### 4.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    客户端（浏览器）                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │  管理端 Admin │  │  学生端 Student│  │  微信小程序   │   │
│  │  (8002/集成)  │  │  (8001/集成)  │  │              │   │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘   │
└─────────┼─────────────────┼─────────────────┼───────────┘
          │ /api/admin/**   │ /api/student/** │ /api/wx/**
          ▼                 ▼                 ▼
┌─────────────────────────────────────────────────────────┐
│              Spring Boot 后端 (端口 8000)                 │
│  ┌─────────────────────────────────────────────────┐    │
│  │              Spring Security                     │    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────────┐  │    │
│  │  │ 登录认证  │  │ 角色鉴权  │  │ RememberMe   │  │    │
│  │  └──────────┘  └──────────┘  └──────────────┘  │    │
│  └─────────────────────────────────────────────────┘    │
│  ┌───────────┐  ┌───────────┐  ┌───────────────────┐   │
│  │ Controller │→ │  Service  │→ │ MyBatis Mapper    │   │
│  └───────────┘  └───────────┘  └─────────┬─────────┘   │
└──────────────────────────────────────────┼──────────────┘
                                           ▼
                                  ┌──────────────┐
                                  │  PostgreSQL   │
                                  │  数据库 (xzs) │
                                  └──────────────┘
```

### 4.2 部署模式

| 模式 | 说明 | 访问方式 |
|------|------|----------|
| **集成部署** | 前端编译后的静态文件放在后端 `resources/static/` 目录，由 Spring Boot 统一托管 | `http://host:8000/admin/index.html`（管理端）<br>`http://host:8000/student/index.html`（学生端） |
| **前后端分离** | 前端通过 Vue CLI devServer 独立运行，API 请求通过 proxy 转发到后端 | 管理端 `http://localhost:8002`<br>学生端 `http://localhost:8001` |

---

## 五、数据库设计

### 5.1 数据库表一览

| 表名 | 说明 | 主要字段 |
|------|------|----------|
| `t_user` | 用户表 | id, user_name, password, real_name, role, status, deleted |
| `t_subject` | 学科表 | id, name, level, level_name, item_order |
| `t_question` | 题目表 | id, question_type, subject_id, score, difficult, correct, info_text_content_id |
| `t_text_content` | 文本内容表 | id, content, create_time（存储题目/试卷的 JSON 详情） |
| `t_exam_paper` | 试卷表 | id, name, subject_id, paper_type, score, question_count, suggest_time |
| `t_exam_paper_answer` | 试卷答案表 | id, exam_paper_id, system_score, user_score, status, create_user |
| `t_exam_paper_question_customer_answer` | 答题记录表 | id, question_id, exam_paper_id, answer, do_right, customer_score |
| `t_message` | 消息表 | id, title, content, send_user_id, receive_user_count |
| `t_message_user` | 消息接收表 | id, message_id, receive_user_id, readed |
| `t_task_exam` | 任务试卷表 | id, title, grade_level, frame_text_content_id |
| `t_task_exam_customer_answer` | 任务答卷表 | id, task_exam_id, create_user, text_content_id |
| `t_user_event_log` | 用户日志表 | id, user_id, user_name, content, create_time |
| `t_user_token` | 用户 Token 表 | id, token, user_id, wx_open_id（微信登录用） |

### 5.2 枚举值定义

**用户角色 (RoleEnum)**

| code | 名称 | 说明 |
|------|------|------|
| 1 | STUDENT | 学生 |
| 3 | ADMIN | 管理员 |

**用户状态 (UserStatusEnum)**

| code | 名称 |
|------|------|
| 1 | 启用 |
| 2 | 禁用 |

**题目类型 (QuestionTypeEnum)**

| code | 名称 | 是否需要 TextContent |
|------|------|---------------------|
| 1 | 单选题 | 否 |
| 2 | 多选题 | 否 |
| 3 | 判断题 | 否 |
| 4 | 填空题 | 是 |
| 5 | 简答题 | 是 |

**试卷类型 (ExamPaperTypeEnum)**

| code | 名称 | 说明 |
|------|------|------|
| 1 | 固定试卷 | 固定题目组成 |
| 4 | 时段试卷 | 限时开放 |
| 6 | 任务试卷 | 由任务分发 |

**答卷状态 (ExamPaperAnswerStatusEnum)**

| code | 名称 |
|------|------|
| 1 | 待批改 |
| 2 | 完成 |

---

## 六、后端架构详解

### 6.1 分层架构

```
Controller（控制器层）
    ↓ 接收请求，参数校验，调用 Service
Service（业务逻辑层）
    ↓ 业务处理，事务管理
Repository/Mapper（数据访问层）
    ↓ MyBatis XML 映射，SQL 操作
Domain（实体层）
    数据库表对应的 Java 实体类
ViewModel（视图模型）
    请求参数 DTO 和响应 DTO
```

### 6.2 Controller 层

#### 管理端 API (`/api/admin/**`)，需要 `ROLE_ADMIN`

| Controller | 路径前缀 | 功能 |
|------------|----------|------|
| DashboardController | `/api/admin/dashboard` | 仪表盘统计 |
| UserController | `/api/admin/user` | 用户管理（CRUD、状态变更） |
| ExamPaperController | `/api/admin/exam/paper` | 试卷管理 |
| QuestionController | `/api/admin/question` | 题目管理 |
| EducationController | `/api/admin/education/subject` | 学科管理 |
| ExamPaperAnswerController | `/api/admin/examPaperAnswer` | 答卷管理 |
| MessageController | `/api/admin/message` | 消息管理 |
| TaskController | `/api/admin/task` | 任务管理 |
| UploadController | `/api/admin/upload` | 文件上传 |

#### 学生端 API (`/api/student/**`)，需要 `ROLE_STUDENT`

| Controller | 路径前缀 | 功能 |
|------------|----------|------|
| DashboardController | `/api/student/dashboard` | 学生首页（任务/固定/时段试卷列表） |
| UserController | `/api/student/user` | 学生个人信息、注册、消息 |
| ExamPaperController | `/api/student/exam/paper` | 试卷列表 |
| ExamPaperAnswerController | `/api/student/exampaper/answer` | 答题提交与查看 |
| QuestionAnswerController | `/api/student/question/answer` | 错题本 |
| EducationController | `/api/student/education/subject` | 学科查询 |
| UploadController | `/api/student/upload` | 文件上传 |

#### 公共 API

| 路径 | 功能 |
|------|------|
| `POST /api/user/login` | 用户登录 |
| `POST /api/user/logout` | 用户登出 |
| `/api/student/user/register` | 学生注册（无需认证） |

#### 微信端 API (`/api/wx/**`)

| Controller | 路径前缀 | 功能 |
|------------|----------|------|
| WxStudentController | `/api/wx/student` | 微信端学生相关接口 |

### 6.3 安全认证机制

```
请求流程:
Browser → DebugCookieFilter → Spring Security Filter Chain → Controller

Spring Security Filter Chain:
  1. SecurityContextPersistenceFilter  -- 恢复 SecurityContext
  2. HeaderWriterFilter                -- 安全头
  3. CorsFilter                        -- 跨域处理
  4. LogoutFilter                      -- 登出处理
  5. RestLoginAuthenticationFilter      -- 自定义 JSON 登录
  6. RequestCacheAwareFilter
  7. SecurityContextHolderAwareRequestFilter
  8. RememberMeAuthenticationFilter     -- Cookie 自动登录
  9. AnonymousAuthenticationFilter
 10. SessionManagementFilter
 11. ExceptionTranslationFilter         -- 异常转换
 12. FilterSecurityInterceptor          -- URL 权限校验
```

**登录流程：**
1. 前端发送 `POST /api/user/login`，body 包含 `userName`、`password`（明文）、`remember`
2. `RestLoginAuthenticationFilter` 拦截请求，解析 JSON body
3. `RestAuthenticationProvider` 验证用户名和密码（密码通过 RSA 加密比对）
4. 验证成功后，`RestAuthenticationSuccessHandler` 返回用户信息
5. 如果 `remember=true`，`RestTokenBasedRememberMeServices` 设置 RememberMe Cookie
6. 后续请求通过 `JSESSIONID`（Session）或 `remember-me`（Cookie）保持登录状态

**密码加密：**
- 使用 RSA 非对称加密
- 公钥/私钥配置在 `application.yml` 的 `system.pwdKey` 中
- 数据库中存储的是 RSA 加密后的密码

**Cookie 配置：**
- Cookie 名称：`xzs`
- 有效期：30 天
- HttpOnly：是

### 6.4 全局异常处理

`ExceptionHandle` 类使用 `@ControllerAdvice` 统一捕获异常：

| 异常类型 | 返回码 | 返回消息 |
|----------|--------|----------|
| `Exception` | 500 | 系统内部错误 |
| `MethodArgumentNotValidException` | 501 | 参数验证错误（含详细字段） |
| `BindException` | 501 | 参数验证错误 |

### 6.5 系统错误码

| code | 常量名 | 说明 |
|------|--------|------|
| 1 | OK | 成功 |
| 400 | AccessTokenError | 用户登录令牌失效 |
| 401 | UNAUTHORIZED | 用户未登录 |
| 402 | AuthError | 用户名或密码错误 |
| 500 | InnerError | 系统内部错误 |
| 501 | ParameterValidError | 参数验证错误 |
| 502 | AccessDenied | 用户没有权限访问 |

### 6.6 事件机制

系统使用 Spring Event 实现异步事件处理：

| 事件 | 监听器 | 功能 |
|------|--------|------|
| `UserEvent` | `UserLogListener` | 记录用户登录等操作日志 |
| `CalculateExamPaperAnswerCompleteEvent` | `CalculateExamPaperAnswerListener` | 自动计算试卷得分 |

---

## 七、前端架构详解

### 7.1 管理端 (xzs-admin)

**端口：** 8002（开发模式）

**页面路由：**

| 路径 | 页面 | 说明 |
|------|------|------|
| `/login` | 登录页 | |
| `/dashboard` | 主页 | 统计数据（试卷数、题目数、答卷数等） |
| `/user/student/list` | 学生列表 | 学生 CRUD |
| `/user/admin/list` | 管理员列表 | 管理员 CRUD |
| `/exam/paper/list` | 试卷列表 | 试卷 CRUD |
| `/exam/paper/edit` | 试卷编辑 | 创建/编辑试卷 |
| `/exam/question/list` | 题目列表 | 题目 CRUD |
| `/exam/question/edit/*` | 题目编辑 | 按题型分为 5 个编辑页面 |
| `/task/list` | 任务列表 | 任务 CRUD |
| `/task/edit` | 任务创建 | |
| `/education/subject/list` | 学科列表 | 学科 CRUD |
| `/answer/list` | 答卷列表 | 查看学生答卷 |
| `/message/list` | 消息列表 | |
| `/message/send` | 消息发送 | |
| `/log/user/list` | 用户日志 | 查看用户操作日志 |
| `/profile/index` | 个人简介 | |

**API 模块：**

| 文件 | 对应后端路径 |
|------|-------------|
| `api/dashboard.js` | `/api/admin/dashboard` |
| `api/user.js` | `/api/admin/user` |
| `api/examPaper.js` | `/api/admin/exam/paper` |
| `api/question.js` | `/api/admin/question` |
| `api/subject.js` | `/api/admin/education/subject` |
| `api/examPaperAnwser.js` | `/api/admin/examPaperAnswer` |
| `api/message.js` | `/api/admin/message` |
| `api/task.js` | `/api/admin/task` |
| `api/login.js` | `/api/user/login`, `/api/user/logout` |

### 7.2 学生端 (xzs-student)

**端口：** 8001（开发模式）

**页面路由：**

| 路径 | 页面 | 说明 |
|------|------|------|
| `/login` | 登录页 | |
| `/register` | 注册页 | |
| `/index` | 首页 | 任务试卷、固定试卷、时段试卷 |
| `/paper/index` | 试卷中心 | 浏览可用试卷 |
| `/record/index` | 考试记录 | 查看历史答卷 |
| `/question/index` | 错题本 | 查看错题 |
| `/user/index` | 个人中心 | 个人信息 |
| `/user/message` | 消息中心 | 接收的消息 |
| `/do` | 试卷答题 | 在线答题页面 |
| `/edit` | 试卷批改 | 查看批改结果 |
| `/read` | 试卷查看 | 只读查看答卷 |

### 7.3 前端请求封装

`src/utils/request.js` 封装了 Axios 请求：
- 所有请求设置 `withCredentials: true`（携带 Cookie）
- 统一处理响应码：
  - `401`（未登录）→ 跳转登录页
  - `502`（无权限）→ 跳转登录页
  - `500`（内部错误）→ reject
  - `501`（参数错误）→ reject
  - `1`（成功）→ resolve

### 7.4 Vuex 状态管理

管理端 Store 模块：
- `app` — 侧边栏状态
- `tagsView` — 标签页视图
- `user` — 用户信息（通过 `/api/admin/user/current` 获取）

---

## 八、配置说明

### 8.1 后端配置 (`application.yml`)

```yaml
server:
  port: 8000                           # 服务端口

system:
  security-ignore-urls:                # 无需认证的 URL
    - /api/wx/**
    - /api/admin/upload/configAndUpload
    - /api/admin/upload/auth
    - /api/student/user/register
  pwdKey:
    publicKey: <RSA公钥>                # 密码加密公钥
    privateKey: <RSA私钥>               # 密码解密私钥
  wx:
    appid: <微信AppID>                  # 微信小程序配置
    secret: <微信Secret>
  qn:
    url: <七牛云URL>                     # 文件存储配置
    bucket: <Bucket名称>
    access-key: <AccessKey>
    secret-key: <SecretKey>
```

### 8.2 数据库配置 (`application-dev.yml`)

```yaml
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/xzs
    username: postgres
    password: 123456
```

### 8.3 前端配置 (`vue.config.js`)

```javascript
devServer: {
  host: '0.0.0.0',
  port: 8002,                          // 管理端端口（学生端 8001）
  proxy: {
    '/api': {
      target: 'http://localhost:8000',  // 代理到后端
      changeOrigin: true
    }
  }
}
```

---

## 九、部署指南

### 9.1 开发环境启动

**前提条件：**
- Java 8+（已验证 Java 11 兼容）
- Maven 3.6+
- PostgreSQL 10+
- Node.js 12+

**步骤：**

```bash
# 1. 创建数据库
sudo -u postgres createdb xzs
sudo -u postgres psql -d xzs -f init-db.sql

# 2. 启动后端
cd source/xzs
mvn spring-boot:run -Dspring-boot.run.profiles=dev

# 3. 启动管理端前端
cd source/vue/xzs-admin
npm install
npx vue-cli-service serve --mode dev

# 4. 启动学生端前端
cd source/vue/xzs-student
npm install
npx vue-cli-service serve --mode dev
```

**访问地址：**
- 管理端：http://localhost:8002 或 http://localhost:8000/admin/index.html
- 学生端：http://localhost:8001 或 http://localhost:8000/student/index.html

### 9.2 集成部署模式

前端编译后复制到后端 static 目录：

```bash
# 编译管理端
cd source/vue/xzs-admin
npm run build
# 输出到 admin/ 目录，复制到 source/xzs/src/main/resources/static/admin/

# 编译学生端
cd source/vue/xzs-student
npm run build
# 输出到 student/ 目录，复制到 source/xzs/src/main/resources/static/student/

# 打包后端
cd source/xzs
mvn clean package -DskipTests
# 运行: java -jar target/xzs-*.jar --spring.profiles.active=dev
```

### 9.3 Docker 部署

```bash
cd docker
docker-compose up -d
```

### 9.4 默认账号

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 管理员 | admin | 123456 | 访问管理端 (admin/index.html) |
| 学生 | （需注册） | - | 通过学生端注册页面创建，或管理端创建 |

> **注意：** 管理员账号（role=3, ROLE_ADMIN）只能访问管理端 `/api/admin/**` 接口。学生账号（role=1, ROLE_STUDENT）只能访问学生端 `/api/student/**` 接口。两个角色互不通用。

---

## 十、核心业务流程

### 10.1 考试流程

```
管理员创建学科 → 管理员创建题目 → 管理员组卷（试卷）
                                        ↓
                              学生登录 → 选择试卷 → 答题
                                                    ↓
                                              提交答卷
                                                    ↓
                                    ┌───────────────┴───────────────┐
                                    ↓                               ↓
                              客观题自动阅卷                   主观题人工批改
                                    ↓                               ↓
                                    └───────────────┬───────────────┘
                                                    ↓
                                              生成成绩
                                                    ↓
                                    学生查看成绩 / 管理员查看统计
```

### 10.2 任务试卷流程

```
管理员创建任务 → 关联多张试卷 → 指定年级
                                    ↓
                    学生首页显示任务 → 逐卷完成 → 任务完成
```

### 10.3 消息流程

```
管理员编写消息 → 选择接收用户 → 发送
                                 ↓
                      学生端消息中心显示 → 标记已读
```

---

## 十一、关键设计要点

### 11.1 题目内容存储

题目详情（选项、答案解析等）以 **JSON 格式**存储在 `t_text_content` 表中，通过 `info_text_content_id` 外键关联。这种设计使得不同题型可以灵活扩展字段。

### 11.2 试卷内容存储

试卷的题目组成以 **JSON 格式**存储在 `t_text_content` 表中，通过 `frame_text_content_id` 关联。JSON 内容描述了试卷的分组（大题）和每组包含的题目 ID。

### 11.3 CORS 配置

后端配置了全局 CORS 支持：
- 允许所有 Origin（`*`）
- 允许所有 HTTP 方法
- 允许携带凭证（`allowCredentials: true`）
- 仅对 `/api/**` 路径生效

### 11.4 文件上传

支持两种存储方式：
- **七牛云对象存储**（生产环境）
- **本地存储**（开发环境）

---

## 十二、已知注意事项

1. **角色隔离**：admin 账号只能访问管理端，student 账号只能访问学生端，不可混用
2. **密码加密**：数据库中的密码是 RSA 加密后的字符串，不是明文也不是 hash
3. **node-sass 兼容性**：高版本 Node.js 需要将 `node-sass` 替换为 `sass`
4. **Cookie 跨域**：通过 IDE 浏览器预览代理访问时，可能出现 cookie 无法传递的问题，建议直接使用浏览器访问 `localhost` 对应端口
5. **集成部署**：`resources/static/` 中的前端文件是预编译版本，修改前端源码后需要重新编译并复制
